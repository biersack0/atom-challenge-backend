name: CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
          cache-dependency-path: functions/package-lock.json

      - name: Install dependencies
        working-directory: ./functions
        run: npm ci

      - name: Build
        working-directory: ./functions
        run: npm run build

      - name: Run tests
        working-directory: ./functions
        run: npm run test
        env:
          APP_FIREBASE_PROJECT_ID: ${{ secrets.APP_FIREBASE_PROJECT_ID }}
          APP_FIREBASE_CLIENT_EMAIL: ${{ secrets.APP_FIREBASE_CLIENT_EMAIL }}
          APP_FIREBASE_PRIVATE_KEY: ${{ secrets.APP_FIREBASE_PRIVATE_KEY }}

      - name: Deploy to Firebase
        if: github.ref == 'refs/heads/main'
        working-directory: ./functions
        run: npm run deploy
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
